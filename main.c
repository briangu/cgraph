#include <stdio.h>

#include "graph.h"

int main(void)
{
  // printf("What is your name? ");

  // char *buffer = NULL;
  // int read;
  // size_t len;

  // read = getline(&buffer, &len, stdin);
  // if (-1 != read) {
  //   buffer[read - 1] = '\0';
  //   printf("Hello, %s.\n", buffer);
  // }
  initialize();
}
